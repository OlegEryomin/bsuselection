<div class="container">
    <h2>{{header}}</h2>
    <form action="/action_page.php">
        {{#options}}
            <div class="form-check">
                <label class="form-check-label">
                    <input type="radio" class="form-check-input">{{text}}
                </label>
            </div>
        {{/options}}
        <button type="submit" class="btn btn-primary">Сохранить</button>
    </form>
</div>